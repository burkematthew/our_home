<div id="error_explanation">
  <h2><%= pluralize(@errors.count, "error") %> prohibited from saving changes to this <%= @label %>:</h2>
  <ul>
    <% @errors.each do |error| %>
      <li><%= error.full_message %></li>
    <% end %>
  </ul>
</div>